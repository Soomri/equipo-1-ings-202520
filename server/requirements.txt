# --- Core dependencies ---
fastapi==0.115.0              # Framework for building APIs
uvicorn[standard]             # ASGI server for FastAPI with extras

# --- Database & ORM ---
sqlalchemy                    # ORM for database models and queries
psycopg2-binary==2.9.9        # PostgreSQL driver (for Supabase or Postgres)
supabase                      # Supabase client

# --- Authentication & Security ---
python-jose[cryptography]     # JWT creation and verification
passlib[bcrypt,argon2]        # Password hashing (bcrypt and Argon2)
argon2-cffi                   # Argon2 hashing support

# --- Environment variables ---
python-dotenv                 # Load environment variables from .env

# --- Email handling & validation ---
email-validator               # Validate email formats
pydantic[email]==2.8.2        # Pydantic with email support
pydantic-core==2.20.1         # Core Pydantic functionalities

# --- Date & Time utilities ---
python-dateutil               # Date parsing and manipulation utilities

# --- Data Science & Prediction ---
pandas                        # Data manipulation
numpy                         # Numerical computations
prophet==1.2.1                # Forecasting for time series
scikit-learn                  # Metrics and evaluation (MAE, RMSE, etc.)
holidays                      # Colombian holidays for Prophet
joblib                        # Model serialization
plotly                        # Visualization library for interactive plots

# --- Testing dependencies ---
pytest                        # Main testing framework
httpx                         # Required for FastAPI TestClient
pytest-asyncio                # Optional, for async endpoints
pytest-benchmark              # Optional, for performance tests

